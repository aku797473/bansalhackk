{
  "name": "smart-kisan-root",
  "version": "1.0.0",
  "scripts": {
    "start": "node prod-server.js",
    "dev": "node start-lite.js",
    "install:all": "npm install --prefix gateway && npm install --prefix services/auth-service && npm install --prefix services/user-service && npm install --prefix services/weather-service && npm install --prefix services/crop-service && npm install --prefix services/fertilizer-service && npm install --prefix services/market-service && npm install --prefix services/labour-service && npm install --prefix services/chatbot-service && npm install --prefix services/news-service && npm install --prefix services/payment-service && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@clerk/express": "^2.1.12",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb-memory-server": "^11.1.0",
    "mongoose": "^8.0.0",
    "morgan": "^1.10.0",
    "rss-parser": "^3.13.0"
  }
}
